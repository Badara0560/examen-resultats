services:
  - type: web
    name: def-resultats-mali
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: ADMIN_PASSWORD
        generateValue: false
